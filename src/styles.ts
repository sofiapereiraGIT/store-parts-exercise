import styled from "styled-components";

export const App = {
  PageContentWrapper: styled.div`
    display: flex;
    padding: 1.34em;
    height: calc(100vh - 2em - (0.67em * 2 * 2) - (1.34em * 2));
    overflow: auto;
  `,
};
